#==============================================#
#>> BeadSorted.Saliva.EPIC RGChannelSet data <<#
#==============================================#

#Local processing

# setwd("E:/saliva_cellprops/data/idats")
# library(minfi)
#
# sheet <- list.files() %>%
#   gsub("_Grn.idat", "", .) %>%
#   gsub("_Red.idat", "", .) %>%
#   unique
#
# BeadSorted.Saliva.EPIC <- read.metharray(basenames = sheet)
# save(BeadSorted.Saliva.EPIC, file = "../BeadSorted.Saliva.EPIC.rda", version = 3, compress = "xz")
